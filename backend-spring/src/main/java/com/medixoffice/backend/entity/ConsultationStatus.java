package com.medixoffice.backend.entity;

/** Backed by ConsultationStatusConverter - "In Progress" isn't a valid Java identifier. */
public enum ConsultationStatus {
    SCHEDULED,
    IN_PROGRESS,
    COMPLETED,
    CANCELLED
}
