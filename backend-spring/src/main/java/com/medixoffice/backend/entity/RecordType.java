package com.medixoffice.backend.entity;

/** Backed by RecordTypeConverter - "Lab Result" isn't a valid Java identifier. */
public enum RecordType {
    CONSULTATION,
    LAB_RESULT,
    PRESCRIPTION,
    SURGERY,
    VACCINATION,
    OTHER
}
